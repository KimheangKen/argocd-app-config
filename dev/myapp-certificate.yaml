apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: web-myapp-cert
  namespace: myapp
spec:
  dnsNames:
    - hw-app.kbaenak.tech
  secretName: app-tls
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
